Режим ask (read-only) прошёлся по `Makefile` и `docker-compose.yml`, собрал список команд и эндпоинтов для проверки живости и предложил готовый текст раздела — но файлы не трогал, потому что не мог. Режим code получил те же факты и edit'ом вставил раздел «Как проверить, что сервис жив» в `README.md` между таблицей команд и разделом API.
Разница по сути: ask думает и объясняет, code делает — ask сэкономил время на разведке, code довёл дело до изменений в репо.